<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Reçoit l'alarme de fin de repos (AlarmManager.setAlarmClock) et joue le son
             elle-même, indépendamment de tout pipeline de notification. -->
        <receiver
            android:name=".RestAlarmReceiver"
            android:exported="false" />

        <!-- Widget d'écran d'accueil : 6 tuiles du tableau de bord, lecture seule,
             tap = ouvre l'app. Voir DashboardWidgetProvider.kt / WidgetBridgePlugin.kt. -->
        <receiver
            android:name=".DashboardWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dashboard_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Minuteur de repos du carnet de muscu : l'alarme doit sonner à la seconde près
         même écran verrouillé. SCHEDULE_EXACT_ALARM couvre Android 12-13 (l'utilisateur
         peut la révoquer dans les réglages), USE_EXACT_ALARM couvre Android 14+ et est
         accordée d'office — justifiée ici car le minuteur est une fonction centrale de
         l'app, et celle-ci n'est pas distribuée sur le Play Store. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
